# Shell scripts must stay LF even on Windows checkouts (core.autocrlf=true),
# because the Gazebo track runs them inside WSL/Linux where CRLF breaks bash.
*.sh text eol=lf
*.py text eol=lf
*.sdf text eol=lf
*.config text eol=lf
